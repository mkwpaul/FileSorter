<Application x:Class="FileSorter.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FileSorter"
             xmlns:converters="clr-namespace:WPF.Common.Converters;assembly=WPF.Common"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <converters:FileInfoToImageSourceConverter x:Key="fileInfoToImage"/>

        <!--
        <SolidColorBrush x:Key="MainBackground" Color="#36393f"/>
        <SolidColorBrush x:Key="SecondaryBackground" Color="#2f3136"/>
        <SolidColorBrush x:Key="TerciaryBackground" Color="#202225"/>
        <SolidColorBrush x:Key="MainForeground" Color="#fafafa"/>
        <SolidColorBrush x:Key="SecondaryForeground" Color="#8a8e92"/>

        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource MainForeground}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="200"/>
        </Style>
        <Style TargetType="{x:Type Label}">
            <Setter Property="Background" Value="{StaticResource MainBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource MainForeground}"/>
        </Style>

        <Style TargetType="{x:Type Control}">
            <Setter Property="Background" Value="{StaticResource MainBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource MainForeground}"/>
        </Style>
        -->

    </Application.Resources>
</Application>
